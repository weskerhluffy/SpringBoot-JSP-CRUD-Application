<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="
    http://www.springframework.org/schema/security 
    http://www.springframework.org/schema/security/spring-security-4.2.xsd
    http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd">

	<http use-expressions="true">
		<intercept-url pattern="/login*" access="isAnonymous()" />
		<intercept-url pattern="/*" access="isAuthenticated()" />

		<form-login login-page='/login' default-target-url="/caca"
			authentication-failure-url="/login?error=true" />

		<logout logout-success-url="/login" />
		<csrf disabled="true" />

	</http>

	<authentication-manager>
		<authentication-provider>
		<!-- XXX: http://consistentcoder.com/spring-security-4-xml-config-jdbc-authentication-annotation-security-method -->
			<jdbc-user-service id="jdbcUserService" 
			data-source-ref="dataSource"
			users-by-username-query=" select nombre as username, contra as password, activo as enabled from usuario where nombre=?"
			authorities-by-username-query=" select u.nombre as username, r.nombre from usuario u, rol r, usuario_rol ur where u.id=ur.usuario_id and r.id=ur.rol_id and u.nombre=?" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>